defaults:
  - _self_

_target_: nbprint.Configuration
name: "basic"

outputs:
  _target_: nbprint.NBConvertOutputs
  naming: "{{name}}-{{date}}-{{datetime}}-{{uuid}}-{{sha}}"
  root: .pytest_cache/garbage

hydra:
  searchpath:
    - file://examples/hydra
