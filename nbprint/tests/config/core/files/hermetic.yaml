defaults:
  - _self_

_target_: nbprint.Configuration
name: "basic"

outputs:
  _target_: nbprint.NBConvertOutputs
  naming: "{{name}}"
  path_root: .pytest_cache/garbage

hydra:
  searchpath:
    - file://examples/hydra

