# @package _global_

content:
  covermatter:
    - _target_: nbprint.ContentMarkdown
      content: |
        # Report Title
        ## Subtitle
      css: ":scope { text-align: center; }"

  table_of_contents:
    - _target_: nbprint.ContentTableOfContents

  frontmatter:
    - _target_: nbprint.ContentMarkdown
      content: |
        # Preface
        This is the preface section of the report.

  middlematter:
    - _target_: nbprint.ContentPageBreak

    - _target_: nbprint.ContentMarkdown
      content: |
        # Main Body
        The main content of the report.

  endmatter:
    - _target_: nbprint.ContentPageBreak

    - _target_: nbprint.ContentMarkdown
      content: |
        # Bibliography
        References go here.
